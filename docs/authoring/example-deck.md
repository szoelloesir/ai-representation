# Example Deck

The canonical sample deck used by the runtime is:

- `src/content/example-deck.md`

It includes:

- narrative frames
- a diagram slide
- a gimmick slide (`Gimmick: context delta`)
- high-level motion presets for each frame
